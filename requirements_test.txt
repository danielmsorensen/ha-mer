pytest-homeassistant-custom-component==0.13.365
# aioresponses 0.7.9 (latest on PyPI) constructs aiohttp's ClientResponse without the
# `stream_writer` kwarg that aiohttp 3.14+ (pulled in transitively by homeassistant 2026.9.x)
# now requires, so every mocked response raises TypeError. Pinned to the fix commit from the
# upstream PR until a release with the fix ships: https://github.com/pnuckowski/aioresponses/pull/288
aioresponses @ git+https://github.com/agners/aioresponses.git@8f40e0db2eed16621607ecfc857168240c795755
ruff==0.14.0
